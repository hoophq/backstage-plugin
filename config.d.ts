export interface Config {
    hoop: {
        /**
        * Configuration options used to generate analytics graphs
        * @visibility frontend
        */
        baseUrl: string;
        token: string;
    };
}
